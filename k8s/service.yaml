apiVersion: v1
kind: Service
metadata:
  name: portfolio-webapi
  namespace: default
  labels:
    app: portfolio-webapi
spec:
  type: ClusterIP
  selector:
    app: portfolio-webapi
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  sessionAffinity: None
